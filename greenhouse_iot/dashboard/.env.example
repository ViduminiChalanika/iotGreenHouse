# Same API key as rest_api/.env (Vite exposes only VITE_* to the client)
VITE_API_KEY=change-this-to-match-rest_api-env

# Optional: where Vite proxies /api in dev (default 127.0.0.1:3000)
# VITE_PROXY_TARGET=http://127.0.0.1:3000
